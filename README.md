# ODD contract package

```
docker build . -t odd-contract:build 
    --build-arg ODD_CONTRACT_VERSION=<VERSION>
    --build-arg TWINE_USERNAME=<TWINE_USERNAME>
    --build-arg TWINE_PASSWORD=<TWINE_PASSWORD>
```